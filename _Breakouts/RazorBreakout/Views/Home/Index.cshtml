@*<ul>
    @for (int i = 0; i < 10; i++)
    {
        <li>@i</li>
    }
</ul>*@

@{string name = "tommy";}

@name

<p>@DateTime.Now</p>

does this work?
@{ 
    string greeting = "";
    if(DateTime.Now.Hour > 12)
    {
        greeting = "Good Evening my kind fellow person";
    }
    else
    {
        greeting = "Good morrow, doth thou wilt the constabulary to vacate itself from this domicile";
    }

   <p> @greeting</p>
}

@{ 
    int[] nums = { 12, 45, 42, 90};
}
<h2>Nums Printed by a foreach loop out:</h2>
@foreach(int num in nums)
{
    <p>@num</p>
}

<h2>Nums printed by a for loop</h2>

@for(int i =0; i < nums.Length; i++)
{
    <p>@nums[i]</p>
}
